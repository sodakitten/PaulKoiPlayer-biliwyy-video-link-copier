# v12.0.0 settings update

- Default parser prefix is `https://danmaku.paulkoishi.com/player/?url=`.
- Backup parser prefix is `https://biliplayer.91vrchat.com/player/?url=`.
- Existing option buttons are unchanged; two new buttons switch the parser prefix and double-click action.
- Default click behavior is single-click parser URL, double-click direct URL.
- Live rooms always use direct URL.
- Multipage videos keep single-click parser URL when using the default parser prefix.
